using System.Collections.Generic;
using Yarn;

namespace UnbeatableTranslations.Translation
{
    class ProgramIndex
    {
        public static Dictionary<string, Program> programs = new Dictionary<string, Program>();

        public static Dictionary<string, string> lines = new Dictionary<string, string>();

        public static UnityTable unityTable = new UnityTable();
    }
}
